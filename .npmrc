; Fail `npm install` immediately on an unsupported Node (engines in package.json).
; Without this, an old Node sails through install and dies at `npm start` with a
; cryptic loader/option error instead of "you need Node >= 22.18".
engine-strict=true
