{
  "name": "cutiemail-site",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "The cutiemail docs + marketing site. Renders the repo's own markdown into a static site deployed to Cloudflare Workers.",
  "scripts": {
    "build": "node build.mjs",
    "dev": "node build.mjs && wrangler dev",
    "deploy": "node build.mjs && wrangler deploy"
  },
  "devDependencies": {
    "markdown-it": "^14.1.0",
    "markdown-it-anchor": "^9.2.0",
    "wrangler": "^4.0.0"
  }
}
